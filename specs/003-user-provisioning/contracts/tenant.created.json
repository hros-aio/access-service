{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "TenantCreatedEvent",
  "type": "object",
  "required": ["tenantCode", "rootAdminEmail"],
  "properties": {
    "tenantCode": {
      "type": "string",
      "description": "Unique identifier of the new tenant"
    },
    "rootAdminEmail": {
      "type": "string",
      "format": "email",
      "description": "Email address of the initial root administrator"
    }
  }
}
